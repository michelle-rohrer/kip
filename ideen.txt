ToDo
- Cursor auf einer VM laden

Projektideen 

Ein Chrome/Edge-Extension, die beim Seitenaufruf lokal im Browser riskante URLs/Seiten beurteilt (Privacy-freundlich) und Nutzer warnt/blockt.

ML-Kern (Anomaly + Klassifikation)
Multistufiger Detektor, alles on-device:
URL-Modell (Deep Learning)
Zeichen-/Token-basierte CNN/Transformer auf der reinen URL (Host, Pfad, Query).
Bewährte Idee: URLNet (CNN für URL-Strings) → adaptieren/neu trainieren, dann nach TF.js exportieren. 
arXiv
Seiten-Feature-Encoder (Autoencoder = Anomaly Detection)
Leichtgewichtiger Autoencoder lernt „normale“ Seitenmerkmale: DOM-Tiefe, Anteil Inline-JS, iframes, Form-Actions, Passwortfelder, on*-Handler, Entropie von Script-Tokens usw.
Rekonstruktionsfehler als Anomalie-Score → flaggt neuartige Angriffe/Zero-Days, die nicht in Feeds stehen.
Regel- & Reputationsschicht
Live-Abgleich gegen lokale PhishTank/OpenPhish-Snapshots (periodisch aktualisiert) → niedrige Latenz, FPs runter. 
Inference im Browser: mit TensorFlow.js (WebGL/WebGPU). Gute Doku + Paper vorhanden. 